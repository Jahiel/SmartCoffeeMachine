name: Simple Test

on: 
  push:
    branches: 
      - "*"

jobs:
  example_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Hello World
        run: echo "GitHub Actions triggered!"
